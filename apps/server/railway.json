{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable pnpm && corepack prepare pnpm@9.0.0 --activate && cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @workspace/types build && cd apps/server && pnpm rebuild duckdb duckdb-async && pnpm exec prisma generate && pnpm exec nest build"
  },
  "deploy": {
    "startCommand": "node dist/main.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE"
  }
}
